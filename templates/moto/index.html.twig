{% extends 'base.html.twig' %}


{% block title "Listes des voitures" %}


{% block body %}

<h3>L'identification de chaque motos</h3>
<ul>

 {% for moto in motos %}
     <li>
        <a href={{ path('app_moto_show', {id: moto.id, slug: moto.marque})}}>{{ moto.marque}}</a>
        {% else %}
        <p> pas encore de motos</p>
        {% endfor %}
        </li>
    </ul>
{% endblock %}
